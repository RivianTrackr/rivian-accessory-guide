{
  "name": "rivian-accessory-guide",
  "version": "1.5.1",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
